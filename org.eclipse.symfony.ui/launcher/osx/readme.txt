the osx launcher script will open the file in eclipse for url schemes like:

symfony://open?file=/Users/Acme/Sites/symfony/index.php

Drag the symfony2eclipse app into your application folder, and it will handle the symfony:// url scheme.